class tree
{
	constructor(x,y,width,heigt)
	{
		var options={
			isStatic:false,
			restitution:0,
			friction:1,
            density:1.2
        }

			display()
	{
			var postree=this.treeBody.position;
			push()
			imageMode(CENTER);
			
			pop()

	}
}
}